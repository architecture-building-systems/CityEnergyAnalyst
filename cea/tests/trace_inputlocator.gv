digraph trace_inputlocator {
    node [shape=box, rankdir=LR];

    {% for direction, script, locator, file in trace_data %}
        {% if direction == 'input' %}
            "{{locator}}" --> "{{script}}"
        {% else %}
            "{{script}}" --> "{{locator}}"
        {% endif %}
    {% endfor %}
}